const nodemailer = require('nodemailer')

const transporter = nodemailer.createTransport({
 host: "smtp.gmail.com",
  port: 587,
  secure: false, 
  auth: {
    user: "romaananyev10@gmail.com",
    pass: "oilo czsm digf dvox",
  },
})

async function send(html)
{
    const to = 'romaananyev10@gmail.com';
    const from = 'romaananyev8@gmail.com';
    const subject = 'MY THEME';

    const info = {
        to: to,
        from: from,
        subject: subject,
        html: html
    };

    await transporter.sendMail(info, (err, info) =>{
        if(err) console.log(err.message);
        else console.log("Message sent:", info.messageId);
    });
    
}

// exports.send = send;
module.exports = { send };